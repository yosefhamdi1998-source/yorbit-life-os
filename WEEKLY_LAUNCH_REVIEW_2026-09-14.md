# Weekly launch review — September 14, 2026

**Not ready for paid launch or App Store submission.** Reviewed master d044bcd plus the deletion-disclosure change. Evidence below separates live configuration, local tests, and customer journeys. No real records, subscriptions or bank connections were changed.

| Area | Current evidence | Remaining launch work |
| --- | --- | --- |
| Bank credentials | Live schema has connected_accounts.access_token_ref and private plaid_credentials.access_token. Aggregate-only check still finds one legacy access-* reference among nine accounts. Edge exchange v9, sync v21 and deletion v9 are active with JWT verification. | Finish legacy-token remediation and test link, update, sync, disconnect and retry using sandbox accounts. No token values were read. |
| User isolation | Live transactions and connected_accounts policies constrain ownership for reads/writes. Subscriptions expose owner SELECT. Profiles constrain id to auth.uid(). Security-definer crypto helpers inspected include caller scoping; delete_all_my_data deletes by auth.uid(). | Two real authenticated synthetic accounts must exercise cross-user reads and writes. Advisor warnings and static checks alone cannot certify isolation. |
| Security configuration | Live advisors still flag pg_net in public, five authenticated-callable security-definer functions and disabled leaked-password protection. Eight RLS/no-policy tables include backend-only tables; this does not itself prove exposure. Storage currently has zero buckets. | Review each privileged function and extension permission intentionally; determine plan/access needed for password protection. Do not remove required RPC access blindly. |
| Signup and native returns | Source builds signup/recovery/provider redirects from window.location.origin. Native plist lacks registered URL schemes and source search found no appUrlOpen handler. | Verify email confirmation, reset and external bank/auth return paths in a signed iPhone build; implement native callback routing where needed. Browser origin behavior is not native verification. |
| Web billing | Live Stripe default portal is active, returns to /settings, permits invoice/payment management and cancellation at period end. Live create-checkout v2, stripe-webhook v2 and billing-portal v1 are active. | Restricted-key/verification gap remains. Test checkout, webhook replay/order, paid access, cancellation and portal login with synthetic customers. Configuration is not a successful purchase. Assess tax obligations/registrations before enabling Stripe Tax. |
| Native paid access | RevenueCat initialization still supplies only apiKey; no account identity supplied. Format preflight passes. | Implement account identity and server entitlement reconciliation; verify purchase/restore/logout/account switching in StoreKit/TestFlight. |
| Account deletion | Actual deployed delete-account v9 swallows Stripe cancellation errors, only cancels locally active subscriptions, and continues deleting local subscription/auth records. Plaid revocation failures also remain non-fatal. Current mocked test checks vault lookup and auth-delete failure, not billing cancellation failure. | Add durable cancellation/revocation recovery before losing identifiers, cover trialing/past_due and missing-key/query failures, and verify synthetic end-to-end deletion. This is a code-level finding, not evidence of an actual charged deleted customer. |
| Exports | Today's full local suite passed, including CSV/import validation, PDF row wrapping and export pagination. | Verify native file export/share and actual sandbox account export completeness. No real financial export was downloaded during this review. |
| AI | Live ai-coach v13 is active; usage failure protection has local mocked coverage. Code still reads profiles.ai_tier separately from subscription state. | Verify provider funding, consent, paid/free policy and concurrency limits. Do not promise paid AI allowances that are unverified. |
| UI and native packaging | Synthetic desktop deletion Cancel works; blank confirmation disables destructive action. Home spending opens the exact Aug 10–Sep 8 report and agrees on $8,048. Chart fits at 1280px. Capacitor app.yorbit, iOS project and privacy manifest exist. | Signed archive, privacy report, SDK inventory, safe areas, keyboard, physical-device layouts, screenshot set and reviewer account remain unverified. |

## Improvement in this release
The account-deletion confirmation now warns that Apple billing continues until cancelled and links directly to Apple's subscription management page. It also directs web subscribers to check cancellation in Settings. Immediate account deletion remains available; this disclosure does not repair the backend recovery gap above. Checked visible text/link, disabled confirmation and Cancel using synthetic data; strict lint, frontend build and existing account-deletion/native tests passed.

## Apple requirements rechecked today
Apple requires account-deletion access and advises explaining continued Apple billing with a subscription-management route. [Account deletion guidance](https://developer.apple.com/support/offering-account-deletion-in-your-app/).

Review needs complete metadata, working backend access and a usable reviewer account/demo. Digital subscription purchase rules depend on storefront and applicable exceptions; do not assume a web checkout alone covers every iOS storefront. [App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/).

## Owner actions versus engineering
Owner: complete required Apple membership/account/legal steps, supply accurate business/privacy details, resolve pending service verification and approved funding, and grant signing/store access where needed. Do not purchase or accept agreements automatically.

Engineering: deletion lifecycle recovery, bank credential migration, native callbacks/identity/entitlements, sandbox payment and two-user isolation tests, signed-device QA and evidence-based submission pack. These are not all tasks to hand back to the owner.
